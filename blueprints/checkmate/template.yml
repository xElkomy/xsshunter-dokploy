variables:
  main_domain: ${domain}

config:
  domains:
    - serviceName: client
      port: 80
      host: ${main_domain}

  env:
    - DOMAIN=${main_domain}
    - EMAIL_PASSWORD=
    - EMAIL_ADDRESS=
    - EMAIL_PORT=
    - EMAIL_HOST=

  mounts: []
