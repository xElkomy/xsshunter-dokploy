[variables]
main_domain = "${domain}"

[config]
[[config.domains]]
serviceName = "poke"
port = 6003
host = "${main_domain}"

[config.env]

[[config.mounts]]
filePath = "/poketube/config.json"
content = """
{
  "server_port": 6003,
  "image_proxy": "" 
}
"""