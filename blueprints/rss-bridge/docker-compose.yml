version: "3.8"

services:
  rss-bridge:
    image: rssbridge/rss-bridge:latest
    restart: unless-stopped
    expose:
      - 80
    volumes:
      - rssbridge-config:/config

volumes:
  rssbridge-config: {}
