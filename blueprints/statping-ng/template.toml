[variables]
main_domain = "${domain}"

[config]
[[config.domains]]
serviceName = "statping-ng"
port = 8080
host = "${main_domain}"

[config.env]
TZ = "UTC"
DB_CONN = "sqlite"

[[config.mounts]]
source = "../files/statping-ng"
target = "/app"